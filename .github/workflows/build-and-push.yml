name: Build and Push Backend Image

on:
  push:
    branches:
      - feature/ci
  pull_request:
    branches:
      - feature/ci

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Create .env file
      env:
        PORT: ${{ secrets.PORT }}
      run: |
        echo "MODEL_PATH=data/models/model.joblib" > .env
        echo "SCALER_PATH=data/models/scaler.joblib" >> .env
        echo "THRESHOLD=0.7" >> .env
        echo "PORT=$PORT" >> .env
        echo "MAX_MB=5" >> .env

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ghcr.io/cucharoth/identity_recognition:latest